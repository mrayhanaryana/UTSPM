import React from 'react';
import { View } from 'react-native';

    const AppFlexDimensions = () => {
        return ( <View style={{flex:1, backgroundColor: 'powderblue'}}>
        </View>
    );
};

export default AppFlexDimensions;